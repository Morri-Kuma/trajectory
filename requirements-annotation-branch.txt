# Dependencies for the annotation-robustness branch (src/).
# Split into "core" (needed for the local smoke test) and "full" (server scANVI).
#
# CORE — sufficient to run scripts/run_smoke_test.py on a laptop (CPU, no GPU):
numpy>=1.24
pandas>=2.0
scipy>=1.10
scikit-learn>=1.3
anndata>=0.9
matplotlib>=3.7
pyyaml>=6.0

# RECOMMENDED — production trajectory/preprocessing (auto-used if present):
# scanpy>=1.10
# leidenalg>=0.10
# igraph>=0.11
# umap-learn>=0.5

# FULL / SERVER — real scANVI + scArches reference mapping (GPU recommended):
# scvi-tools>=1.1
# torch>=2.0           # install the build matching your CUDA/CPU
# scarches>=0.6        # optional; scvi-tools provides SCANVI.load_query_data

# Existing benchmark methods (already vendored under benchmark/methods/):
# scNODE, PRESCIENT, MIOFlow, WOT, CellRank2 — see benchmark/README.md
