# Local smoke-test notes

**Date:** 2026-06-09 · **Command:** `python scripts/run_smoke_test.py` · **Result:** PASS

## What was actually run
- **Inputs:** *synthetic* tiny AnnData generated by `scripts/make_synthetic_smoke_data.py`
  (reference 1,500 cells; gse178325 1,200; gse230659 1,100; 60 shared genes incl. 11 canonical markers).
  **No real data was downloaded.**
- **Annotation:** the **KNN-on-PCA surrogate** (scvi-tools/torch absent in the local env), tagged `annotation_method = knn_surrogate`.
- **Pipeline:** load → joint preprocess (sklearn backend) → annotate → six comparisons → trajectory (geodesic/surrogate DPT + surrogate PAGA) → tables + figures.
- **Outputs:** `results/test_outputs/annotation_branch/{gse178325,gse230659}/` (13 files each) + `summary.json`.

## Observed numbers (synthetic — illustrative only, NOT biological results)

| metric | gse178325 | gse230659 |
|---|---:|---:|
| cells | 1,200 | 1,100 |
| pseudotime Spearman (A vs B) | 0.804 | 0.822 |
| agreement NMI | 0.229 | 0.239 |
| correspondence overall agreement | 0.656 | 0.682 |
| composition TV (mapped) | 0.706 | 0.717 |
| fraction flagged unknown/OOD | 0.62 | 0.65 |
| graph edge Jaccard (A vs mapped-B) | 0.07 | 0.10 |
| disagreement cells | 899 | 838 |

## Interpretation (of the *plumbing*, not biology)
- The pipeline executes end-to-end and emits every planned table and figure.
- The planted structure reproduces the intended qualitative pattern: **high pseudotime
  correlation** (main axis robust) with **substantial intermediate disagreement** and a
  **high OOD fraction** (the deliberate OSKM-reference ↔ chemical-query mismatch).
- These values come from synthetic data with a strict surrogate confidence threshold;
  they validate the *code paths and metrics*, nothing about real reprogramming.

## What this does NOT establish
- No biological conclusion. Real results require `mode: server` with scvi-tools + GPU on the
  full GSE242424 / GSE178325 / GSE230659 `.h5ad` inputs and the real `final_milestone_label_coarse`
  provider as the marker-silver annotation.

## Reproduce
```bash
pip install -r requirements-annotation-branch.txt
python scripts/run_smoke_test.py
```
